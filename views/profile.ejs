<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Household Profile</title>
</head>

<body>

  <h1>Household Profile</h1>

  <p>User: <%= user.firstName %> <%= user.lastName %></p>

  <form action="/household/update" method="POST">

    <label>Address</label>
    <input type="text" name="address" value="<%= household.address || '' %>" />

    <br>

    <label>Latitude</label>
    <input type="text" name="latitude" value="<%= household.latitude || '' %>" />

    <br>

    <label>Longitude</label>
    <input type="text" name="longitude" value="<%= household.longitude || '' %>" />

    <br>

    <label>Phone Number</label>
    <input type="text" name="phone_number" value="<%= household.phone_number || '' %>" />

    <br>

    <label>Readiness Level</label>
    <select name="readiness_level">
      <option value="low" <%= household.readiness_level === 'low' ? 'selected' : '' %>>Low</option>
      <option value="medium" <%= household.readiness_level === 'medium' ? 'selected' : '' %>>Medium</option>
      <option value="high" <%= household.readiness_level === 'high' ? 'selected' : '' %>>High</option>
    </select>

    <br>

    <label>Notes</label>
    <textarea name="notes"><%= household.notes || '' %></textarea>

    <br>

    <button type="submit">Save</button>
  </form>

  <a href="/dashboard">Back to dashboard</a>

</body>
</html>